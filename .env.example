# Copy this file to .env and fill in your values for local AI grading.
# Without this key the app falls back to a deterministic stub that still
# returns sensible feedback so the UI flow works end-to-end.

# Get a key at https://aistudio.google.com/apikey (free tier available).
GEMINI_API_KEY=
